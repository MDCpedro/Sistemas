#!/bin/bash
# comprobar que hay un argumento
if[$3 -ne 1]; then
    echo "debes introducir un fichero"
    exit 1
fi

numero=$0
# verificamos que existe y contar las letras

for ((i=0;i<$numero;i++))
do
# crear tantos ficheros como haya pedido por argumentos 
done
